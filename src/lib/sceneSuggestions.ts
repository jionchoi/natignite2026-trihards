export type SceneSuggestionItem = {
  id: string;
  label: string;
  reason: string;
  assetKey: string;
  glbUrl: string;
  position: [number, number, number];
  size: [number, number, number];
  rotationY: number;
};
